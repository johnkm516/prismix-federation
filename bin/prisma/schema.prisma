// *** GENERATED BY PRISMIX :: DO NOT EDIT ***
datasource db {
	provider = "postgresql"
	url = "DATABASE_URL"
}
generator client {
	provider = "prisma-client-js"
}
model Post {
	id Int @id @default(autoincrement())
	title String 
	content String 
	account_id Int 
	account Account @relation(name: "AccountToPost", fields: [account_id], references: [id])
	@@unique([content, title])
	@@unique([title, id])
	@@map("posts")
}
model Account {
	id Int @id @default(autoincrement())
	posts Post[] @relation(name: "AccountToPost")
	username String 
	email String 
	status String 
	@@map("accounts")
}
model User {
	id Int @unique
	firstname String 
	lastname String 
	@@id([firstname, lastname])
}
model Product {
	id Int @id @default(autoincrement())
	name String 
	price Int 
}
enum Role {
	USER
	ADMIN
}