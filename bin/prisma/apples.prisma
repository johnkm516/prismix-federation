datasource db {
  provider = "postgresql"
  url      = "DATABASE_URL"
}

generator client {
  provider        = "prisma-client-js"
}

model Apple {
  seeds   Int
  core    String  @unique
}

// models examples
model User {
  id        Int       @unique @default(1)
  createdAt DateTime  @default(now())
  email     String    @unique
  name      String?
  password  String
  whale     String
  cat       String
}
